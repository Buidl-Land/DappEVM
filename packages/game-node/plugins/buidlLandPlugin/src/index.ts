// Export the plugin class with the desired name, even if the filename is different for now
import BuidlLandPlugin from "./BuidlLandPlugin"; // Pointing to the old filename

export default BuidlLandPlugin;